<%= form_with(model: easybooking) do |form| %>
  <% if easybooking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(easybooking.errors.count, "error") %> prohibited this easybooking from being saved:</h2>

      <ul>
        <% easybooking.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :phone_number %>
    <%= form.text_field :phone_number %>
  </div>

  <div class="field">
    <%= form.label :date_of_booking %>
    <%= form.date_select :date_of_booking %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
